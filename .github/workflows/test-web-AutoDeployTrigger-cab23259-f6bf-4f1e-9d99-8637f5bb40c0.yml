name: Trigger auto deployment for test-web

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ live ]
    paths:
    - '**'
    - '.github/workflows/test-web-AutoDeployTrigger-cab23259-f6bf-4f1e-9d99-8637f5bb40c0.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.TESTWEB_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.TESTWEB_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.TESTWEB_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: formula1registry.azurecr.io
          registryUsername: ${{ secrets.TESTWEB_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.TESTWEB_REGISTRY_PASSWORD }}
          containerAppName: test-web
          resourceGroup: Formula1ResourceGroup
          imageToBuild: formula1registry.azurecr.io/test-web:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


